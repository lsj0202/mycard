export const colors = {
  red: '#f44336',
  blue: '#2196f3',
  green: '#4caf50',
  white: '#fff',
  black: '#212121',
  gray: '#9e9e9e',
}

export type Colors = keyof typeof colors // key값들만 사용 -> keyof
